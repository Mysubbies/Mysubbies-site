{
  "name": "mysubbies-backend",
  "private": true,
  "version": "1.0.0",
  "description": "Serverless payment/payout functions for the Mysubbies static site. The HTML pages at the repo root are unaffected by this — no bundler, no framework, still deployed as-is. This file exists only so Vercel installs dependencies for the /api functions.",
  "scripts": {
    "test": "node --test",
    "check:syntax": "node scripts/check-syntax.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "@supabase/supabase-js": "^2.45.0",
    "pdf-lib": "^1.17.1",
    "stripe": "^17.0.0"
  }
}
